import { Heart, Smile, Wallet, TrendingUp } from 'lucide-react';
import type { GameState } from '@/engine/Types';
export const money = (n: number) => new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', maximumFractionDigits: 0 }).format(n);
export default function TopStatusBar({ game }: {
    game: GameState;
}) { const m = game.metrics; return <div className="status-grid"><div className="stat"><span className="stat-icon health"><Heart size={19}/></span><div><span className="stat-label">Health</span><strong>{Math.round(m.health)}<small> / 100</small></strong><div className="mini-track"><i style={{ width: `${m.health}%`, background: '#82916b' }}/></div></div><span className="stat-caption">{m.health > 70 ? 'Feeling good' : 'Need a meal'}</span></div><div className="stat"><span className="stat-icon happy"><Smile size={19}/></span><div><span className="stat-label">Happiness</span><strong>{Math.round(m.happiness)}<small> / 100</small></strong><div className="mini-track"><i style={{ width: `${m.happiness}%`, background: '#c5a153' }}/></div></div><span className="stat-caption">{m.happiness > 60 ? 'Living a little' : 'Take a breather'}</span></div><div className="stat"><span className="stat-icon cash"><Wallet size={19}/></span><div><span className="stat-label">Available cash</span><strong>{money(m.cashBalance)}</strong><span className="stat-detail">Checking account</span></div></div><div className="stat"><span className="stat-icon worth"><TrendingUp size={19}/></span><div><span className="stat-label">Net worth</span><strong>{money(m.cashBalance + game.savedTotal)}</strong><span className="stat-detail">{money(game.savedTotal)} tucked away</span></div></div></div>; }
